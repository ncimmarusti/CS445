package com.example.cs445_project;

/**
 * This class is used to estimate the cost of the ride using google maps api and the start and end
 * locations.
 */
public class RideCostEstimator {

    // Method to calculate the estimated cost of the ride based on start and end locations
    public static double calculateEstimatedCost(Location startLocation, Location endLocation) {
        //Use the google maps api
        return 0;
    }

    // Method to calculate distance between two locations (you need to implement this)
    private static double calculateDistance(Location startLocation, Location endLocation) {
        // Implement logic to calculate distance between two locations
        return 0;
    }
}